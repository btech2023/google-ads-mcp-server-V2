apiVersion: v1
kind: Service
metadata:
  name: google-ads-mcp-server
  namespace: test
  labels:
    app: google-ads-mcp-server
    environment: test
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: google-ads-mcp-server 